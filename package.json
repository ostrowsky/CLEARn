{
  "name": "clearn",
  "private": true,
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd platform && pnpm run build",
    "test": "powershell -ExecutionPolicy Bypass -File ./web/tests/run-tests.ps1",
    "vercel-build": "cd platform && pnpm --filter @clearn/client build"
  }
}
