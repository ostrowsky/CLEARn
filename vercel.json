{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd platform && corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd platform && corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm --filter @softskills/client build",
  "outputDirectory": "platform/apps/client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
